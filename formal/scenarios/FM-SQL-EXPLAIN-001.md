# FM-SQL-EXPLAIN-001 scenarios

The model covers planning-only success, analyzed success with zero or multiple
writes, analyzed failure with rollback, and cancellation before execution.
Cancellation after synchronous execution starts is outside the v0.8.11 model.

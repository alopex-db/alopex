CONSTANTS
  Clients = {c1, c2, remote1}
  RemoteClients = {remote1}
  MaxSequence = 3

INIT Init
NEXT Next

INVARIANTS
  TypeOK
  CommittedValuesAreUnique
  CommittedPrefixIsGapFree
  RemoteNeverAllocates
